version: '3'
services:
  ex0bot:
    build: .
    restart: unless-stopped
    environment:
      - 'PYTHONUNBUFFERED=1'
  redis-server:
    image: 'redis'
    volumes:
      - $PWD/redis_data:/var/lib/redis